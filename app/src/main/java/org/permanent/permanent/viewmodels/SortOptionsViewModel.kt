package org.permanent.permanent.viewmodels

import android.app.Application

class SortOptionsViewModel(application: Application) : ObservableAndroidViewModel(application) {

    fun onSortOptionClicked(option: Int) {
    }
}